from fastapi import APIRouter

router = APIRouter()


# 재고 예측 담당자가 여기 채워주세요.
# 권장 구조: 같은 폴더에 schema.py / data.py / model.py 만들어서 사용.
#
# 예시:
# @router.post("/predict")
# def predict(req: PredictRequest) -> PredictResponse:
#     ...
